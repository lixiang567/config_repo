zuul:
  sensitive-headers:
  routes:
    aaa:
      path: /myProduct/**
      serviceId: product
      sensitive-headers:
  ignored-patterns:
    - /**/product/listForOrder

management:
  security:
    enabled: false
hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 5000
